import { i as a } from "../chunks/lit-element.DRlPF2me.js";
const i = a`
  :host {
    --padding: 0;

    display: none;
  }

  :host([active]) {
    display: block;
  }

  .tab-panel {
    display: block;
    padding: var(--padding);
  }
`;
export {
  i as default
};
