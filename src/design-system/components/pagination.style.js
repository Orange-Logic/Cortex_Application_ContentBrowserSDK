import { c as a } from "../chunks/component.styles.BLcT4bOa.js";
import { i } from "../chunks/lit-element.DRlPF2me.js";
const o = i`
  ${a}

  :host {
    --width: 100px;
    display: block;
  }

  cx-typography::part(base) {
    margin-block: 0;
  }

  .pagination {
    display: flex;
    align-items: center;
    gap: var(--cx-spacing-small);
    font-size: var(--cx-font-size-small);
  }

  .pagination .pagination__label__container {
    display: inline-flex;
    gap: var(--cx-spacing-small);
    align-items: center;
  }

  .pagination .pagination__select::part(combobox) {
    width: var(--width);
  }

  .pagination .pagination__navigation-buttons {
    display: flex;
    align-items: center;
    gap: 0;
  }
`;
export {
  o as default
};
